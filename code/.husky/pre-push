echo "[Husky] Checking for ESLint errors and formatting using Prettier."

# Navigate to the code directory
cd code || exit

# Get the list of files changed since the last push
CHANGED_FILES=$(git diff --name-only $(git merge-base HEAD $(git rev-parse --abbrev-ref --symbolic-full-name @{u})) | grep -E '\.(js|jsx|ts|tsx|css|scss|json|md)$')

# Run ESLint on changed files
if [ -n "$CHANGED_FILES" ]; then
  echo "Running ESLint..."
  cd code
  npx eslint $CHANGED_FILES
  if [ $? -ne 0 ]; then
    echo "ESLint found issues. Aborting push."
    exit 1
  fi
else
  echo "No JavaScript/TypeScript files to lint."
fi

# Format the changed files using Prettier
if [ -n "$CHANGED_FILES" ]; then
  echo "Running Prettier..."
  cd code
  npx prettier --write $CHANGED_FILES
  git add $CHANGED_FILES
else
  echo "No files to format."
fi

